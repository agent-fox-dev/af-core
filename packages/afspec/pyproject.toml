[project]
name = "afspec"
version = "0.1.0"
description = "Spec format models, validation, rendering, and I/O (stub)"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
